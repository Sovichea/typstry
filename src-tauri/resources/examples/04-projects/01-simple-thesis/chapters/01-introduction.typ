// @standalone-preview
= Introduction <introduction>

Large documents are easier to maintain when each chapter has its own source file. This thesis uses three chapters while preserving one document-wide numbering and reference system.

The workflow is demonstrated in @method: open that chapter directly and Typstella displays placeholders for references whose labels belong to other chapters. The complete references remain correct when `main.typ` is compiled.

The final implications are summarized in @conclusion.
