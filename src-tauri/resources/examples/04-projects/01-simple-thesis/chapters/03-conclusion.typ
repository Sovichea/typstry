// @standalone-preview
= Conclusion <conclusion>

The example shows that a multi-file thesis can use ordinary Typst labels across chapter boundaries. The key result in @main-result follows from the chapter structure introduced in @introduction.

Open each chapter from the explorer and edit a reference. Typstella keeps the standalone preview available; open `main.typ` to inspect the final document-wide references and numbering.
